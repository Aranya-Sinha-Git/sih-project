# Data workstream

`validation-datasets/` holds raw public sources, processed datasets, manifests, download status, duplicate analysis, and reports. Source data is kept separate from application code and model-training code.

See `validation-datasets/README.md` before running the data-processing tools.
